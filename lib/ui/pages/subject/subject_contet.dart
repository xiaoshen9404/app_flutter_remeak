import 'package:flutter/cupertino.dart';

class SubjectContent extends StatelessWidget {
  const SubjectContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("SubjectPage"),
    );
  }
}
